const fs = require('fs');

// We are going to replace the Assigned Teacher and Class Time blocks in StudentFormModal.js with a new custom SmartScheduleBuilder component.
// First, we need to see exactly where to inject it.
